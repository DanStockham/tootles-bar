import React, { Component, PropTypes } from 'react';

/**
 * SocialMedia
 */
export class SocialMedia extends Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>MY COMPONENT</div>
    );
  }
}
