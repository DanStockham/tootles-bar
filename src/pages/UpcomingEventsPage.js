import React, { Component, PropTypes } from 'react';
import UpcomingEvents from '../components/UpcomingEvents'

/**
 * UpcomingEvents page
 */
export default class UpcomingEventsPage extends Component { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <UpcomingEvents />
    );
  }
}
